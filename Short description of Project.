Firstly, This blockchain project contains a list of nodes, each having the particular data.

Each node is identified with their previous hash values and the hash value is generated by taking the entire block of previous node. 
This nature describes justifies the immutable nature of the block chain nodes. That lets the security in the network.

Lastly, the code is deployed on the postman.
